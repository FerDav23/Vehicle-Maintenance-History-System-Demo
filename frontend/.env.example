# Demo mode: when true, no real backend is used; all data is mocked locally.
VITE_DEMO_MODE=true

# API base URL (only used when VITE_DEMO_MODE is false)
# Use a generic placeholder for demo; never put production/staging URLs here.
# Example: /api  or  https://demo-backend.example.com
VITE_API_URL=/api
